// You should create an array of numbers in a Javascript file, if there isn't one.
// Write a funciton that multiplies them togehter.

var arrayOfNumbers = [2,2];

function multiply(array) {
  let total = (array[0] * array[1]);
  return total;
}

function sumOfNums(array) {
  let summage = (array[0] + array[1]);
  return summage; 
}
